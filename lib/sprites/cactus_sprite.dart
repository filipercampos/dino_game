import 'sprite.dart';

class CactusSprite extends Sprite {
  CactusSprite({
    String imagePath,
    double imageWidth,
    double imageHeight,
  }) : super(
          imagePath: imagePath,
          imageWidth: imageWidth,
          imageHeight: imageHeight,
        );
}
